/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EditeRolesService } from './editeRoles.service';

describe('Service: EditeRoles', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EditeRolesService]
    });
  });

  it('should ...', inject([EditeRolesService], (service: EditeRolesService) => {
    expect(service).toBeTruthy();
  }));
});
